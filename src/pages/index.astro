---
import Layout from "../components/Layout.astro";
import { getPublishedPosts, readingTime } from "../lib/posts";

const allPosts = await getPublishedPosts();
const perPage = 10;
const totalPages = Math.ceil(allPosts.length / perPage);
const page = 1;
const posts = allPosts.slice(0, perPage);

---

<Layout title="WAHSUN ｜ Blog" description="在伺服器與思緒之間遊走。">
  <section class="hero">
    <div class="hero-inner container">
      <p class="label"><span class="d"></span> 關於作者</p>
      <p class="bio">一個對世界充滿好奇心的人，在伺服器與思緒之間遊走。維護一些安靜運行的服務，寫一些個人使用的程式碼，讀一些讓自己變得更清醒的東西。</p>
      <div class="actions">
        <a href="#posts" class="btn btn-p">閱讀文章</a>
        <a href="mailto:x@wahsun.org" class="btn">聯繫郵箱</a>
      </div>
    </div>
    <div class="scroll-hint"><a href="#posts"><div class="scroll-pill"><span></span></div></a></div>
  </section>

  <section class="posts-section container" id="posts">
    <div class="sec-head">
      <h2>最新文章</h2>
      <span class="pl-date" style="font-size:.8125rem;color:var(--fg3)">第 1 頁 / 共 {totalPages} 頁</span>
    </div>
    <div class="posts">
      {posts.map((p,i) => (
        <a href={`/posts/${p.id}`} class="post-item" style={`transition-delay:${i*50}ms`}>
          <div>
            <div class="pl-meta">#{i + 1}</div>
            <div class="pl-title">{p.data.title}</div>
            <div class="pl-desc">{p.data.description}</div>
              <div class="pl-tags">
                {(p.data.tags||[]).slice(0,3).map(t => <span class="pl-tag">{t}</span>)}
              </div>
          </div>
          <div class="pl-date">{
            p.data.date.toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"})
          }</div>
        </a>
      ))}
    </div>
    <nav class="pager">
      <span></span>
      <a href="/page/2" class="btn">下一頁 →</a>
    </nav>
  </section>
</Layout>
</Layout>
